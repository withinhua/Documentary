/**
 * Product identity in one place. The product name isn't decided yet, so change it here and the
 * Studio, window title and editor chrome follow.
 */
export const BRAND = {
  name: "Documentary Studio",
  tagline: "Topic in, documentary out. Follow every stage, then edit anything.",
  /** Built on OpenReel (MIT). Keep this attribution; the licence requires the notice. */
  poweredBy: "Editor engine based on OpenReel (MIT)",
} as const;
